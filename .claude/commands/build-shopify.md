---
description: Run the full Shopify theme build workflow
argument-hint: [design sources and scope]
---

Task: $ARGUMENTS

Run the startup checklist, then follow the full pipeline in
`workflow/PIPELINE.md` and the relevant phase/source files. Initialise only the
artefacts needed for this full build. The main session owns store/theme safety,
user decisions and final evidence.
