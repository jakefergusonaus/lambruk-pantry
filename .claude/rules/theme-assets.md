---
paths:
  - "assets/**"
---

Reuse the base theme's CSS/JavaScript architecture. Inspect computed runtime
behaviour before overriding selectors or variables. Scope changes narrowly,
preserve reduced motion and progressive enhancement, and avoid introducing a
parallel framework or token system.
