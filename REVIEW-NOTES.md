# Review notes

Running list of visual tweaks spotted during review. Add to this as you go — we'll work through entries in batches rather than one at a time.

Mark an entry `[x]` once it's fixed.

| Done | Page | Element | What's wrong | What it should be |
|---|---|---|---|---|
| [ ] | Shop grid (collection pages), mobile | Product card | Collapsed to the same responsive card used on desktop (1:1 image → 4:3, 10px radius → 16px, sans title → serif, floating pill quick-add → none) for now. **Different from the other two mobile gaps found this build** — those were absences (nothing there at all); this is a fully specified alternative design in the source, right down to a deliberate-looking floating pill quick-add button overlaid on the image. Needs judging on a real phone before calling it settled, not assumed to be an oversight like the others. |
| [x] | Header (sitewide) | Main nav | **Not a bug — design-source gap.** Current-page and hover states built for the main nav (both `#131A3E`, aria-current-driven) have no basis in `LambrukPantry Desktop.dc.html`, which specifies no nav interaction states at all. Design source needs updating to show these states so they're not invisible to the next person reading it. See `DESIGN-TOKENS.md` #10. |
| [ ] | Homepage, `section_two_up_cards` | High Tea card image, Wholesale card image | Both set `image_ratio: "custom"` with `custom_image_ratio: 1.6`, but `blocks/image.liquid`'s ratio logic only has cases for `landscape`/`portrait`/`adapt` — `"custom"` isn't handled, so it silently falls through to the `ratio == null` fallback and renders square (1:1) regardless of the 1.6 value sitting right next to it in the JSON. Pre-existing, not urgent — found 2026-08-25 while diffing `templates/index.json` against the live theme for an unrelated reconciliation, not something either recent session touched. | Probably `landscape` or `adapt`, not `custom` — `custom_image_ratio` isn't a real supported setting on this block at all, so either implement it in `image.liquid` or repoint these two cards at a ratio value that actually renders. |
| [x] | Shop All / category pages | Category eyebrow ("TEA · 12 TEA BAGS") | **Not a bug — design source is stale, not the build.** `LambrukPantry Desktop.dc.html`'s Shop All screenshot shows a category eyebrow on the product cards; the client's actual, current decision (2026-08-25, superseding this) is that the eyebrow appears on occasion pages and the Top Sellers rail only, not Shop All or the three category pages — see `ARCHITECTURE.md` §33. This is the fourth time this exact scope has been revisited; flagging so the design source itself gets corrected rather than continuing to silently disagree with the build every time someone reads it fresh. | Design source's Shop All mockup should drop the eyebrow from its product cards, matching the current, final decision — not something for the theme build to chase again. |
| [ ] | Cafe page | "View Menu" (hero), "View Cafe Menu" (Pull Up a Chair), "View High Tea Menu" (High Tea) buttons | **Blocked on content, not a build gap.** All three buttons appear in `LambrukPantry Desktop.dc.html` with no `onClick` handler at all — unlike every other button in the file, which links to a real route. Checked `Site Map.dc.html` and the package `README.md` too: no menu page, PDF, or route exists anywhere in the design source for any of the three. Built the three sections without these buttons rather than pointing them at `#`, the homepage, or any other dead/wrong destination — a non-functional menu button on a cafe page is worse than no button (2026-08-25). | Once real menu content exists (a page, a PDF, or an external link), add the three buttons back with a real destination each. Until then, nothing to build — this needs content, not code. |
| [x] | Sitewide | Tea / Condiments / Pantry collections | **Resolved 2026-08-26 — client renamed the collections in Admin** to `Tea Collection`, `Sauces & Chutneys`, `Pantry Staples` (handles unchanged: `tea`, `condiments`, `pantry`), matching the footer/Cafe-tile copy exactly. Verified live: Cafe page pantry tiles and all three collection page H1s now read the new names. Checked the design source first, per the client's request, for what each collection page's own H1 should read — found the design has **no per-category page heading at all**: the header only ever toggles based on curated-occasion state (`occasionPlain`/`occasionActive`), never on tag-filter state, and the Shop All filter chips' `set: () => this.setState({ filter: f, occasion: null })` handler never touches header state either. These three collection pages (and their H1s) are the build's own construct, not something the design specifies — so there was no design-vs-build conflict to reconcile, just three Admin-only display names to align. One nuance found, out of scope to fix here: the Pantry collection's browser-tab/SEO title still reads "Pantry", not "Pantry Staples" — likely a separate Admin SEO override field the rename didn't touch. | None — closed. If the stale Pantry SEO title matters, it's a one-field fix in Admin (Collection → Search engine listing), not a theme change. |
| [x] | Our Story | "How we make things" trio (eyebrow, heading, 3 icon cards) | **Not a bug — decided against, not re-flagged.** `LambrukPantry Mobile.dc.html` drops the icon images and the "The Lambruk Promise"/"How we make things" section header entirely on mobile for this one instance (compare `Mobile.dc.html:687-700` to `Desktop.dc.html:896-917`), while the identical icon+heading+copy trio shape is built and shown on every breakpoint for its other two instances (Product sourcing row, Wholesale Benefits). Decision (2026-08-26): keep the header and icons on mobile here too, for consistency with those other two — dropping a section's own heading on mobile would leave three unlabelled items, which reads more like mockup simplification (this one screen not getting full mobile-detail attention) than a deliberate content decision. | None — closed. Design source's mobile mockup is the odd one out among three otherwise-identical instances; not chasing it. |
| [x] | Contact | "Wholesale & stockists" card button label | **Design itself is inconsistent between breakpoints — resolved by picking one, not chasing both.** `Desktop.dc.html:955` labels this button "Wholesale Details"; `Mobile.dc.html:732` labels the identical button, same destination, "Wholesale Enquiry." Built as "Wholesale Details" on both breakpoints (2026-08-26) — matches the desktop copy, and keeps this button's naming pattern consistent with the adjacent "Cafe Details" button rather than introducing a third distinct label. | None — closed. If the client has a preference between the two, it's a one-word copy change in `templates/page.contact.json`, not a structural one. |
| [x] | Occasion pages | Filter chips + item count above the product grid | **Resolved 2026-08-27 — excluded via `template.suffix`.** `blocks/lambruk-tag-filter.liquid` self-hides based on tag diversity (renders only when 2+ of tea/condiments/pantry are present in the collection's own filters) — a heuristic built for Shop All/category pages that unintentionally backfired on occasion pages, since a curated occasion deliberately mixes products across categories, tripping the same "2+ tags" check it was meant to satisfy. Design source confirmed directly (`Desktop.dc.html:321-330`): the whole chips-and-count row is wrapped in `{% if occasionPlain %}`, absent whenever a curated occasion is being viewed. Added an explicit `template.suffix != 'occasion'` exclusion. Also closes the "6 items" vs "6 products" wording complaint for this page specifically — that text lives in the same block, gated by the same condition, so it disappears along with the chips; the underlying items/products wording gap on Shop All itself is unrelated and still open. | None — closed. Verified live: occasion pages show no filter row at all; Shop All's own chips ("All Tea Sauces & Chutneys Pantry & Gifts 52 items") unaffected. |
| [x] | Occasion pages | Product grid 4-up, design specifies 3-up | **Resolved 2026-08-27 — `product_card_size` "medium" → "large" in `templates/collection.occasion.json` only.** The grid uses `repeat(auto-fill, minmax({{ card size }}, 1fr))`, not a fixed column count — "medium" resolves to a 250px minimum, which auto-fills to 4 columns at this theme's ~1240px content width; "large" (340px) auto-fills to exactly 3, matching the design's explicit `grid-template-columns: repeat(3, 1fr)` (`Desktop.dc.html:331`). Confirmed live: 3 columns (381px each) on desktop, 2 on mobile (unchanged, `mobile_product_card_size` untouched). | **Closed sitewide 2026-08-30 — see new Shop All row below.** Client confirmed the scrolling tradeoff was worth it; Shop All and the three category pages now match. |
| [x] | Shop All, category pages (Tea/Sauces & Chutneys/Pantry) | Product card — title/price typeface, size, colour; image ratio; column count | **Resolved 2026-08-30 — full card-sizing pass on `templates/collection.all.json` and `templates/collection.json`.** Title/price were rendering at the wrong size because their `type_preset` (`rte`/`h6`) wasn't `"custom"` — `snippets/text.liquid` only applies a block's own explicit `font`/`font_size`/`text_color` when `type_preset == "custom"`, so the explicit values already sitting in the JSON were silently ignored under any other preset. Fixed: `type_preset: "custom"` on both, title → 24px `var(--font-heading--family)` (was 14px sans), price → 16px `var(--font-body--family)` (unchanged, only the preset was wrong). Also found and fixed, not in the original ask: title and price colours were swapped (`#4A5478`/`#131A3E` backwards relative to `ProductCard.jsx`). Image ratio: `assets/lambruk-tokens.css`'s existing `[data-image-ratio="square"]` override was silently non-functional — a plain (non-`!important`) external rule can't beat `card-gallery.liquid`'s own inline `--gallery-aspect-ratio` declaration on the same element; added `!important`, matching Horizon's own precedent for the same technique in `editorial-product-grid.liquid`. Column count: `product_card_size` "medium" → "large", same mechanism as the occasion-page fix above, now 3-up matching design. Verified live, both breakpoints, all affected templates. | **Side effect, disclosed not chased:** the aspect-ratio CSS fix is global, so it also corrected the occasion template's image ratio (1:1 → 4:3) as a bonus. Occasion's price was left on the same broken `h6` preset at first — **closed 2026-08-30**, `type_preset` → `"custom"` in `collection.occasion.json`, now 16px Geist sans matching the other three templates. Occasion's title (22px, its own `custom` preset, close to but not exactly the 24px used elsewhere) was already correct in mechanism and left as-is. |
| [x] | Our Story icon trio, Wholesale icon trio, 404 heading | Inert `font` values under a non-`custom` preset | **Resolved 2026-08-30 — deleted, not fixed, after live-checking they were harmless.** `page.our-story.json` (`item_farm`/`item_fruit`/`item_batches`), `page.wholesale.json` (`item_blending`/`item_supply`/`item_formats`), and `404.json`'s `text_GqmVFf` H1 all carried an explicit `font` value under a native (non-`"custom"`) `type_preset` — mechanically the same shape as the Shop All card bug, but `snippets/typography-style.liquid`'s gate only drops `font`/`font_size`/`weight`/`line_height`/`letter_spacing`, not `text_color` (that's set unconditionally by `text.liquid` itself — see `ARCHITECTURE.md` §44's mechanism correction). Checked each: the icon trio's stored `var(--font-subheading--family)` was moot because `--font-heading--family` and `--font-subheading--family` currently resolve to the same font file in this theme's settings; the 404 H1's stored `var(--font-body--family)` was actually *wrong* (native `h1` correctly uses the `heading`/serif role) and being dropped was accidentally saving it from a regression. Removed all seven inert `font` lines outright rather than leave dead config next to a preset that will never read it — before/after computed style (family, size, weight, line-height, letter-spacing, colour) captured on one trio heading and the 404 H1, zero change confirmed. | **If the trio headings are ever meant to render in the *subheading* role specifically (not `heading`), that's a deliberate design decision to make and re-author** — not a value to leave sitting inert in the JSON assuming it'll someday activate. Nothing to action unless that decision is made. |
| [x] | Footer General column (`text_general_links`) | `text_color` alpha didn't match design | **Resolved 2026-08-30 — `#F7F5F2B8` (72%) → `#F7F5F2C7` (78%),** matching the design source's `rgba(247,245,242,.78)`. Found while auditing the `type_preset` bug class above — not that bug (this property isn't gated), just a value that was never checked against the design. Verified live: computed `rgba(247, 245, 242, 0.78)` exactly. | None — closed. The block's `font_size` (14px live vs. design's 15px) is a separate, deliberately-accepted 1px gap — see `DESIGN-TOKENS.md`'s 2026-08-30 concession note. |
| [ ] | `article.json`, `blog.json`, `cart.json`, `list-collections.json`, `page.json`, `password.json`, `product.json`, `search.json` (18 blocks total) | `font`/`font_size` set under a non-`custom` `type_preset` | **Not a bug today — these blocks are byte-identical to the pristine Horizon baseline (`b2d7c4b`), confirmed by diff, not assumed.** None of these pages have been designed yet, so there's nothing of ours being silently dropped right now. Flagging for whoever designs these pages next: **authoring a `font` or `font_size` value on any of these blocks without also switching `type_preset` to `"custom"` will be silently dropped**, same mechanism as the Shop All card bug and the trio/404 case above (`ARCHITECTURE.md` §44). | **Re-check when this page is designed.** Nothing to build now — this is a standing trap for whenever these templates get real content, not an open bug. |
| [x] | Sitewide | Footer Shop / Explore columns | **Resolved 2026-08-26 — footer converted from hardcoded HTML to native Shopify menus.** `sections/footer-group.json`'s Shop and Explore columns were literal `text` blocks with raw `<a href>` HTML — editable only by a developer, and silently stale whenever a linked page/collection was renamed (exactly what had just happened to the three collections above). Replaced both with native `menu` blocks bound to two new Admin-managed menus (`Footer Shop` → handle `footer-shop`, `Footer Explore` → handle `footer-explore`), with the block's own `heading` setting pinned to `"Shop"`/`"Explore"` so the displayed column titles stay stable regardless of what the menus are named in Admin. Verified live on 182395437357: all 4 Shop links (All Products, Tea Collection, Sauces & Chutneys, Pantry Staples) and 4 Explore links now resolve to 4 distinct real destinations each — before this change all 5 Shop links pointed at `/collections/all` regardless of label. Notable finding: "All Products" resolves to `/collections/all-products`, a separate collection from `/collections/all`. Confirmed correct link count and both headings on mobile too. Native `heading_preset` initially rendered the menu heading as a large serif h3 instead of the site's small-caps gold "eyebrow" style used by the untouched General column — fixed with a scoped `custom-liquid` `<style>` override (nested inside `group_shop`, targeting `.menu__heading` within this footer section only, reusing the existing `--letter-spacing--body-eyebrow` token) so all three column headings now compute identically. General column deliberately left as a plain `text` block, not converted — two of its three lines (phone, address) have no URL, and a Shopify menu requires every item to have one. Gift Boxes link deliberately dropped from the Shop menu — no Gift Boxes collection exists yet, and a link pointing somewhere approximate would repeat the same mistake as a dead `#` link. | None — closed for Shop and Explore. If/when a Gift Boxes collection is created, add it to the `Footer Shop` menu in Admin — no theme change needed. |
| [x] | Shop All, category pages, occasion | Product card — no Add-to-cart control at all | **Resolved 2026-08-30 — new `_lambruk-add-button` block, price row.** Design wanted a persistent "Add" button next to price; Horizon's only native equivalent was a hover-revealed circular pill overlaid on the card image, invisible on mobile by default. Built a new (not forked) block that calls Horizon's own unmodified `quick-add` snippet — no variant/availability logic reimplemented — restyled to spec and placed as a sibling of `price` inside a native `_product-card-group` row. The old hover pill is suppressed (not restyled) via a `:has()` selector scoped to cards containing the new block, so the homepage Top Sellers rail and product-page recommendations keep Horizon's exact default behaviour, confirmed live (0 leaks either surface). See `ARCHITECTURE.md` §45 for the full build, including the two bugs found (zero-width wrapper from an unstyled custom element, a cart icon the design didn't call for) and the complete verification (computed style, 375px, an actual cart.js add-to-cart confirmation, card bottom spacing re-measured). | None — closed. |
| [x] | Shop All, category pages, occasion | **Price row when a product has a `compare_at_price` (sale/discount pricing)** | **Read this before the client's first sale goes live — this has never been seen against real data.** No product in the catalog has ever had a `compare_at_price` set (checked all 52 via `/products.json`, 2026-08-30); every measurement here is simulated by injecting `snippets/price.liquid`'s actual sale-price markup into a live card, not observed on a real discounted product. **Shipped behaviour: wrap, not hide.** At narrow (~375px, 2-up grid) widths, when a sale price doesn't fit next to the button on one line, the price row wraps — both the current price and the struck-through original stay fully visible on their own line, and the Add button drops to a second line beneath, full size, no truncation. Chosen over an earlier "hide the compare-at price" version that was fully built and verified first: hiding kept the layout on one line but removed the only visual signal that an item is discounted, on a majority-mobile site, for the exact shoppers on the narrowest cards it mattered most for. Confirmed before switching that the layout risk which would have justified keeping "hide" doesn't actually apply — CSS Grid stretches every card in a shared row to match the tallest one, so a wrapped card never misaligns its row; a non-sale neighbour just gains a little dead space to match, the same dynamic this grid already tolerates from variable title-wrap lengths. Implementing wrap surfaced a real, separate bug in Horizon's own `quick-add` component (nested custom elements that were only ever sized for the original image-overlay context resolved to 0 width once wrapped, anchoring the button 58px off to the left) — fixed with a single scoped CSS property, not a chain of width overrides; see `ARCHITECTURE.md` §45 for the full trace. **Verified after switching:** single-price cards (all 52 products today) are pixel-identical to before this change — same 13 live products measured at 375px, zero wrapped, zero overlap, reference card's button position and price-gap matched the pre-change baseline to the decimal. | **Re-verify against a real sale product the first time one exists.** Nothing else to action until then — this is the shipped, verified behaviour, not a pending decision. |
