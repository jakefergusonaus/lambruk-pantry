The only form field in the system — used for newsletter capture and wholesale enquiry.

```jsx
<Input label="Email" placeholder="Enter your email" hint="Seasonal dispatches, never spam." />
```

48px tall, 6px radius. Focus draws a gold ring; errors switch the border to `--gold-700`.
