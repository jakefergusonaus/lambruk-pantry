Closing email-capture block. Lives on the navy band above the footer.

```jsx
<Newsletter onSubmit={(email)=>subscribe(email)} />
```

Defaults carry the brand's own copy. `tone="light"` for a paper band. Uses `Input` + `Button` internally — don't rebuild the field.
